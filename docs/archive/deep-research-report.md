# Pre-Project Engine Selection Research — Best Options for a Drinking Water Digital Twin Built Like a Game

> [!IMPORTANT]
> **Historical. Non-binding. Not authoritative.**
>
> Pre-project research comparing game engines and stacks. Predates the architecture. First committed 2026-07-03.
>
> Current status: see [ROADMAP.md](../ROADMAP.md). Authority order: see [INDEX.md](../INDEX.md) §6.

## Bottom line

Your idea is technically sound, and it does **not** need CFD to be useful. What you described is much closer to a real-time **process-flow and level-balance simulator** than to a physics-heavy fluid model: a chain of unit processes where inflows, outflows, valve positions, pump states, and control logic determine inventory, level, spill risk, drain-down, and downstream feed conditions. That fits drinking-water plants well, because utilities commonly use a **series of treatment steps** such as coagulation, flocculation, sedimentation, filtration, and disinfection, and the backbone of a simple tank or basin simulation is just the dynamic material balance \(dV/dt = Q_{in} - Q_{out}\); for a constant-area vessel, that becomes \(A\,dH/dt = Q_{in} - Q_{out}\). citeturn15view8turn15view9

For your specific goal, the strongest options are: **Godot** if you want the best all-around open-source game-engine workflow; **Phaser plus TypeScript** if you want a browser-first simulation that feels more like an interactive web app; **GDevelop** if speed of prototyping matters more than architectural purity; and **Bevy** if you want a more serious long-term simulation core in Rust. **Box3D is not where I would start**: it is a new, low-level **3D rigid-body** library written in C17, and its own manual explicitly says it should not be your first C project. That makes it interesting later as a specialized subsystem, but not the best first foundation for a modular water-plant twin. citeturn15view2turn21view0turn16view4turn21view1

## Why the concept fits a game engine

The core of your concept maps naturally onto game technology because a game engine already gives you the things your twin needs most: an update loop, visual state changes, modular scene/object composition, event signaling, input handling, and quick iteration. In Godot, for example, an application is built as a tree of **nodes** grouped into reusable **scenes**, and nodes communicate with **signals**; that is almost exactly the structure you want for modular plant units such as a sedimentation basin, rapid-mix basin, filter block, clearwell, pump station, or inter-stage channel. citeturn15view0

A clean abstraction for your project would be to treat each unit process as a reusable module with: a geometry profile or storage curve, one or more inlet ports, one or more outlet ports, controllable assets such as valves and pumps, automation state such as manual/auto or simple PID-style rules, and downstream handoff variables such as flow, elevation, and optional water-quality placeholders. That also matches the reality that drinking-water treatment is a treatment **train** rather than one monolithic machine. For your fidelity target, the model can stay simple: a basin or tank only needs flow balance and basic constraints to show operators what happens when a valve is shut too far, an outlet is starved, or a downstream module cannot accept the incoming flow. citeturn15view8turn15view9

This is also why a “video game backend” is a good instinct. You need **stateful simulation with visual consequences**, not a computational-fluid solver. If later you want more realism, you can layer in things like travel delay, head-loss approximations, valve curves, pump curves, filter run-time states, and backwash sequences without abandoning the same architectural model. The game-engine choice matters less than whether the engine makes it easy to represent many discrete systems, connect them cleanly, and visualize them clearly. citeturn15view0turn15view3turn16view2

## What your two repo references are good for

The `awesome-gamedev-agent-skills` repository is genuinely useful for the way you want to work. Its current release describes **66 specialized skills plus a master router**, with engine coverage that explicitly includes **Godot**, **web engines** such as **Phaser** and **PixiJS**, and **Bevy**, and its compatibility docs say the `SKILL.md` format works across agents including **Claude Code** and **OpenAI Codex**. In practical terms, this means you can standardize how your AI tools scaffold tasks such as scene setup, UI wiring, asset import, camera work, or behavior implementation without hand-writing a different system prompt every time. citeturn19view2turn19view0turn19view1

`box3d`, by contrast, is better read as inspiration than as your default starting stack. The official Box3D documentation describes it as a **3D rigid-body simulation library for games**, with collision routines and spatial-query support, written in **portable C17**. The current documentation page is for **Box3D 0.1.0**, and the manual explicitly warns that it should not be your first C project. That tells me two things. First, Box3D is real and promising. Second, it is still early enough that you should expect thin engine integrations, limited tutorials, and more systems glue than you want for a first-pass plant twin. citeturn15view2turn2search3

So the right way to use those two references is asymmetric. Use the **agent-skills repo** immediately to raise the floor on Claude Code and Codex output. Use **Box3D** only if, later, you discover you truly need a custom low-level physics layer for gates, moving hardware, or physically interactive plant props. For the simulation logic you described—levels, volumes, spill/drain outcomes, and modular process connections—Box3D is optional at best. citeturn19view0turn19view2turn15view2

## Best engine and stack options

**Godot is the best overall fit for most versions of this project.** It is fully open source, built around reusable scenes and nodes, and lets a single project mix **GDScript** and **C#** if you want simple scripting in some modules and more formal typed code in others. For your use case, Godot’s composition model is especially strong because a sedimentation basin, filter cell, pump skid, or flow-control chamber can each be a reusable scene, then nested into larger plant scenes. If you want a 2D or isometric presentation, Godot’s TileMapLayer workflow is optimized for grid-based level building; if you want low-poly or voxel-like 3D, GridMap lets you place meshes on a grid and supports optional collision and navigation shapes. citeturn15view0turn15view1turn21view2turn21view3

There is one important caveat with Godot: if you expect the **main deliverable to run in a browser**, do **not** center the project on **Godot 4 C#**. Godot’s web-export documentation says projects written in C# using Godot 4 **currently cannot be exported to the web**. Godot’s web export does support WebAssembly/WebGL 2.0, so if browser deployment matters, I would pair Godot with **GDScript**, not C#, or else keep Godot as a desktop app. For a desktop-first prototype, Godot remains my strongest recommendation. citeturn8search2turn17view0turn17view1

**Phaser plus TypeScript is the best browser-first option.** Phaser is a fast open-source HTML5 framework for WebGL and Canvas; the docs say you can use **JavaScript or TypeScript**, and the official install docs say Phaser ships TypeScript definitions that editors such as VS Code detect automatically. Phaser’s **Scene** model is well suited to this application because each scene has its own display list, update loop, input handling, and loader, and multiple scenes can run simultaneously—for example, a plant-view scene plus a UI-overlay scene. If your first deliverable should be easy to share as a URL with no install friction, Phaser is a very serious option. citeturn14search2turn21view1turn15view3

If you like the browser idea but want something less “game” and more “interactive industrial dashboard,” then **PixiJS** is the alternative worth considering. PixiJS describes itself as an open-source 2D rendering engine for visually rich web applications and explicitly mentions **data visualizations** alongside games. That makes it attractive if your twin will look more like a process mimic or SCADA-inspired UI than a traditional game screen. The tradeoff is that PixiJS is more of a renderer than a batteries-included gameplay framework, so you will build more architecture yourself than you would in Phaser. citeturn16view1turn10search3

**GDevelop is the fastest path to a working prototype if you want to move visually first and code second.** GDevelop positions itself as an open-source, no-code engine with an event-based system, reusable behaviors, AI assistance, and export to desktop, web, and mobile. Its docs are especially relevant to your modular idea: behaviors can be used to keep logic modular, custom extensions can be created directly inside a project, and JavaScript blocks or JavaScript-powered extensions can be mixed with visual events. That means you could represent each plant subsystem as a behavior or extension, while still using JS for the simulation kernel where necessary. citeturn15view4turn16view4turn18view0turn18view1turn18view2

**Bevy is the strongest long-term simulation-core choice, but it is not the easiest prototype path.** Bevy describes itself as a data-driven Rust engine where all engine and game logic uses Bevy ECS, and its web examples page shows that Bevy examples can run in the browser via **WASM + WebGL**, with separate WebGPU examples as well. That architecture is excellent if you envision many plant objects, tags, state resources, systems, and future plug-in logic. But Bevy’s own setup docs also make clear that you need Rust and additional development-environment setup first. So I would choose Bevy only if you already want Rust for maintainability, performance, or long-term simulation rigor—not because it is the fastest way to get the first plant mockup on screen. citeturn16view2turn16view3turn21view0

**Luanti is the niche option if you decide the world itself should be voxel-native.** Luanti says you can create your own voxel game using its Lua API without worrying about voxel rendering or networking, and it has a large mod ecosystem. That makes it attractive only if you want the plant to live inside a broader explorable voxel environment, perhaps with training-game elements, walk-through inspection, or multiplayer tours. If your primary interface is a process diagram, isometric plant, or compact control-room view, Luanti is probably more world than you need. citeturn15view5

## Art, data, and automation layers

For art direction, I would bias toward **isometric 2D** or **low-poly 3D** before pure voxel, unless the voxel look is part of the appeal. Isometric art is faster to read for levels, gates, channels, and basins; low-poly 3D gives you enough depth to show elevations and overflow paths without forcing you into a full navigation game. If you do choose Godot, TileMapLayer is strong for grid-based 2D/isometric layouts, while GridMap is strong for modular 3D block placement. citeturn21view2turn21view3

For ready-made assets, **Kenney** offers thousands of free assets, **Quaternius** offers large CC0 low-poly libraries, **KayKit** ships CC0 blocky/isometric-friendly sets, and **OpenGameArt** remains a broad marketplace for 2D, 3D, textures, audio, and more. For custom voxel creation, **Goxel** is an open-source voxel editor with cross-platform support and many export formats including glTF2, while **MagicaVoxel** is free and lightweight but not open source. These are all practical choices for a prototype, and the fastest route is often to start with Kenney or Quaternius, then only author custom assets where the plant-specific geometry really matters. citeturn16view6turn16view7turn5search2turn16view9turn20search0turn20search11

An optional but very good architectural move is to keep **automation logic** slightly separate from the rendering layer. **Node-RED** is a low-code, event-driven tool for collecting, transforming, visualizing, and automating real-time data, and **MQTT** is a lightweight publish/subscribe standard widely used in industrial and IoT contexts. In a practical stack, the game engine can own spatial state and visuals, while Node-RED owns alarm logic, sequencing, or operator-mode logic, and MQTT moves messages such as valve commands, setpoints, levels, alarms, and simulated telemetry between them. That keeps the twin modular and gives you an easier path to integrating mock PLC logic or live tags later. citeturn15view6turn15view7turn12search1

## Recommended shortlist

If I were prioritizing **the best first build for your exact idea**, my shortlist would look like this.

**Best overall:** **Godot 4 plus GDScript**, using reusable scenes for each unit process and either **TileMapLayer** for an isometric/2D view or **GridMap** for a low-poly modular 3D view. This gives you the best balance of open-source tooling, visual editor support, modular architecture, and AI-assisted coding friendliness through the skills repo. It is the stack I would choose if the project’s center of gravity is “a modular plant simulator with game-engine ergonomics.” citeturn15view0turn21view2turn21view3turn19view2

**Best browser-first:** **Phaser plus TypeScript**, optionally with **PixiJS-style custom rendering patterns** if you want more dashboard-like visuals. Choose this if the primary goal is easy sharing, quick deployment, and a control-room-like interface that runs directly in the browser. Phaser’s scene structure and TS support make it a particularly good fit for an interactive process map with overlays, trends, and alarms. citeturn15view3turn21view1turn16view1

**Best fastest prototype:** **GDevelop**, especially if you want to prove the concept visually before you harden the architecture. The combination of event sheets, reusable behaviors, JS blocks, and built-in AI assistance makes it the fastest way to test whether the operator interaction model is compelling. I would choose this only if speed of demonstration matters more than having the cleanest long-term codebase. citeturn16view4turn18view0turn18view1turn18view2

**Best long-term engineering core:** **Bevy plus Rust**. This is the choice if you see the project growing into a more formal simulation platform with many entities, explicit state systems, and stricter code structure. It is powerful, but it carries more setup cost and less editor convenience up front. citeturn16view2turn16view3turn21view0

**Not my first choice:** **Box3D as the foundation**. It is new, low-level, rigid-body focused, and aimed at developers comfortable in C. I would keep it in the “maybe later” bucket, not the “start here” bucket. citeturn15view2turn2search3

If you want the single clearest recommendation: **pick Godot if you want the best all-around open-source engine for a modular plant twin, or pick Phaser plus TypeScript if you know the product should live in the browser from day one.** citeturn15view0turn21view1